{% extends ':admin:base.html.twig' %}

{% block body %}
  {{ form_start(form) }}
    <div class="form-group">
      <p>Are you sure you want to delete "<strong><em>{{ unit.unit }}</em></strong>" unit?</p>
      {{ form_widget(form.delete, {'attr': {'class': 'btn btn-danger'}}) }}
    </div>
  {{ form_end(form) }}
{% endblock %}